# Packet P5-docs-ssr

Implement:
- `experimental/externalDocs` for built-in ASP/VBScript symbols
- `experimental/ssr` as a narrow simple identifier replacement

Keep SSR scoped and avoid broad structural pattern matching.
